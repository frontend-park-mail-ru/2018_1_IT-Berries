- let n = 1;
- if (listNumber < 3) n = ((listNumber - 1) < 1) ? 1 : listNumber - 1;
- else if (listNumber > (listCount - 2)) n = ((listNumber + 1) > listCount) ? listNumber - 3 : listCount - 4;
- else n = listNumber - 2;
- if (n < 1) n = 1;
- var max_n = n + 5;
if listCount != 0
    if listNumber != 1
        a(href='' class='scoreboard__paginator-left' name='paginator-link' value=listNumber - 1)='<'
        |&nbsp;
    while n <= listCount && n < max_n
        if n != listNumber
            a(href='' value=n class='scoreboard__paginator-link' name='paginator-link')= n++
        else
            b= n++
        |&nbsp;
    if listNumber != listCount
        a(href='' class='scoreboard__paginator-right' value=listNumber + 1 name='paginator-link')='>'
        |&nbsp;
